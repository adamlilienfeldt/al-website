// ─── Site-wide display settings ───────────────────────────
// Toggle these to show/hide fields across the entire site.

export const SHOW_TYPE    = false;  // single / album / ep badge
export const SHOW_YEAR    = true;   // release year
export const SHOW_CREDITS = true;   // credits line
export const SHOW_LABEL   = true;   // label line

export const DARK_MODE    = true;   // set to false to disable dark mode site-wide
export const FORCE_DARK   = false;  // set to true to force dark mode (for testing)
